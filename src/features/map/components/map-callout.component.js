import React from "react";
import { CompactEventInfo } from "../../../components/event/compact-event-info.component";

export const MapCallout = ({ event }) => {
  return <CompactEventInfo event={event} />;
};
